## Basic Elements

This slide shows:

- `#` and `##` headings
- bullet lists and ordered lists
- regular paragraph text

Example editing rules:

1. Add slides under `slides/` using the `NN_NN_slug.md` format
2. Update title and Reveal options in `config.json`
3. Put images and supporting files under `assets/`
